var random = {
    //字符串截取
    getRandomNum:function(min,max ) {
      return Math.floor(Math.random() * (max - min + 1) + min)
    },
    getRandom:function() {
      return Math.random() 
    }
}
  
  module.exports = {
    getRandomNum: random.getRandomNum,
    getRandom:random.getRandom
  }